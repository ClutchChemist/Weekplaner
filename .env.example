# ⚠️ SECURITY WARNING: NEVER commit this file with real values
# ⚠️ A Google API key was previously exposed in Git history
# ⚠️ See SECURITY.md for important information
#
# Copy this file to .env and add your actual Google Maps API Key
GOOGLE_MAPS_KEY=your_google_maps_api_key_here
PORT=5055

# Maps proxy hardening (recommended for production)
# Comma-separated list of allowed browser origins for CORS
# Example: ALLOWED_ORIGINS=http://localhost:5173,https://clutchchemist.github.io
ALLOWED_ORIGINS=

# Optional shared token required via X-Proxy-Token header
# Leave empty to disable token auth (not recommended for public production endpoints)
MAPS_PROXY_TOKEN=

# Basic in-memory rate limiting per IP+route
MAPS_PROXY_RATE_LIMIT_WINDOW_MS=60000
MAPS_PROXY_RATE_LIMIT_MAX=120

# Frontend API base URL for production deployments (e.g. Render proxy URL)
# Leave empty for local Vite dev (uses /api via vite proxy)
VITE_API_BASE_URL=

# Optional: if MAPS_PROXY_TOKEN is set on proxy, frontend must send same value
VITE_MAPS_PROXY_TOKEN=

# Supabase Cloud Sync (optional)
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
# Optional explicit magic-link redirect URL (must be allowed in Supabase Auth settings)
# Example local:  http://localhost:5173/
# Example prod:   https://<user>.github.io/Weekplaner/
VITE_SUPABASE_REDIRECT_TO=
